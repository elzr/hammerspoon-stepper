# Session: 20260427-53e68d

---
*2026-04-27 12:34* <!-- e5e77272-690e-4a3f-b0e4-7bcf2865cf25 -->

## Eli:
there's a weird bug that's not stepper's fault (I don't think so) but I wonder if you could help me fix it: in the right corner of the screen there's a message, probably from a hover popup of some app that is always on top and refuses to get cleaned no matter what I do: [Image #1], it reads "L020-throwaway-custom-domain". It's annoying how obstrusive it is. Can we make it go away?
[Image #1]

## Claude:
Findings so far:
- Not a stale Hammerspoon canvas (no live `hs.canvas` objects)
- Not a standard window in any AX tree (Hammerspoon enumeration finds nothing matching `L020`/`throwaway`)
- Current screenshot of your screen does **not** show the tooltip — it appears to have cleared

**Best hypothesis**: it's a **kitty terminal tab-bar hover tooltip** (macOS-rendered). The `L020-throwaway-custom-domain` text exactly matches the pattern of one of your `features/L020-…` folders, which kitty would show as a tab title when you `cd` there. macOS renders hover tooltips for these via AppKit, and they're known to get stuck when focus changes mid-hover. They don't appear in `hs.window.allWindows()` or in the AX tree because they're transient AppKit help tags, not real windows.

Two questions before I touch anything:

1. **Is the tooltip still visible right now?** My screenshots don't show it. If it cleared on its own, the recurrence is what we want to fix.
2. **Do you confirm it appears over a kitty tab bar?** The teal background in your screenshot matches your kitty theme. If yes, I can add a kitty config tweak (e.g. shorten or disable tab tooltips, or use a non-truncating tab title template) to prevent it. If it's actually some other app (BTT, Raycast hover, Dock tab preview, etc.), tell me which and I'll target that instead.


---
*2026-04-27 12:38* <!-- 81afff90-455b-4b4e-982d-c7d9ce4d558c -->

## Eli:
no it hasn't cleared, you were getting confused most likely bc the tooltip fades over a black CC terminal but I've moved it so it's above a green window: [Image #2], same as before

I confirm it appears over EVERYTHING, it's a pesky little thing: it goes away when I do expose but comes right back after
[Image #1]

## Claude:



---
*2026-04-27 12:39* <!-- e808025d-d199-43da-bb76-2f19131ce873 -->

## Eli:
no it hasn't cleared, you were getting confused most likely bc the tooltip fades over a black CC terminal but I've moved it so it's above a green window: [Image #2], same as before

I confirm it appears over EVERYTHING, it's a pesky little thing: it goes away when I trigger exposé but comes right back after
[Image #1]

## Claude:
The `AXHelpTag` only exposes `AXRaise` — no AX-level dismiss. Hide didn't work. The remaining options are app-level:

**Source confirmed: Bloom (the Setapp launcher) — bug in the app where it leaked an AppKit help tag.** This is why nothing you tried worked: the tag is owned by Bloom but isn't tied to its main window's lifecycle, so window-focus changes don't dismiss it.

Three ways forward, in order of decreasing gentleness:

1. **`Cmd+Q` Bloom and relaunch it** — definitely works; you lose nothing meaningful (Bloom's a launcher, no in-flight state).
2. **Try a synthetic click on the tag's exact frame** `(1544, 1196)` — clicks sometimes scrub stuck help tags. Worth one shot before quitting.
3. **Live with it / file a Bloom bug** — least invasive.

I'd suggest #2 first (one click, fully reversible), then #1 if it doesn't work. Want me to try the click?

