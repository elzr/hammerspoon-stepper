# Session: 20260516-8e9b48

---
*2026-05-16 16:39* <!-- 0f476924-4bd9-4fa8-ac41-94d8ea01927d -->

## Eli:
I'm really liking the squeezing at screen-edge of L010, so much that I think we should rename it to move-to-resize (dropping the -on-single-screen) and let's start enabling it too in the mode where I have my native screen and one external monitor (I think we already enabled it for native + iPad, which is a very related case). Let's also move all the current markdown files to original-vision/ (you don't have to rework them, they're historical documents at this point) and then make a proper README.md that documents simply what's currently implemented and how, a document that should help us recover the vital current context.

## Claude:
All six tasks complete. Hammerspoon reloaded cleanly and `selfTest()` reports `17/17 passed`. The user is currently in `dual` config (Built-in Retina + LS37D70xE), so the new dispatch path is already live — try a fn+arrow toward an edge to see a per-screen shove with the red squeeze flash.

**Changes:**
- `lua/move-to-resize-on-single-screen.lua` → `lua/move-to-resize.lua` (header comment refreshed)
- `features/L010-move-to-resize-on-single-screen/` → `features/L010-move-to-resize/`, with `design.md`/`plan.md`/`where-were-we.md` moved into `original-vision/` untouched
- `lua/stepper.lua`: `currentDisplayConfig()` gains a `"dual"` return (2 screens, no Sidecar), and `dispatchStepMove` routes single/sidecar/dual through `ofsr.shove`; 3+ screens still fall back to vanilla `WinWin:stepMove`
- New `features/L010-move-to-resize/README.md` documents shove math, when it fires, per-app floor, visual feedback, key files, and how to verify
- Top-level `README.md`, `features/L011-.../README.md`, and `.gitignore` updated to the new paths

