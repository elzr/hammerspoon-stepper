# Session: 20260421-f40499

---
*2026-04-21 10:46* <!-- 82d35324 -->

## Eli:
let's review the live window toggles. I was reviewing L007 and right now I think it works only on 4 key "slots" X, Q, A, Z. But the only reason for that was me having trouble remembering the other ones. But now with L009's keymap I want to push this functionality further. I want every key that's not used by Bear note launchers (N,R,D,W,T,S,M) to be assignable. Oh and let's also exclude 1-5 since those toggle Bear highlights and also exclude the General actions with hyper too. This feels like a straightforward change. The whole reason we keep the Bear note launchers separate is bc they have caret & scroll remembering, but can that functionality be added when I add a live window toggle of a Bear note? Give this an ultrathink, do some intense research, find out the edge cases, ask me probing questions and push back were you feel its warranted. Then after a couple of turns we’ll plan this out.

## Claude:
I have the full picture. Before asking probing questions, the most important finding: **the Bear caret/scroll persistence you're worried about is already wired into live slots.** Let me lay out what I found, then push on a few things.

## What's actually in the code

**Current hyper bindings (all keys with any hyper binding today):**
- Bear note launchers (8): N, R, D, W, T, S, M, **I** — you forgot I (`_index 2026`) in your list
- URL launchers (2) in the same `bear-notes.jsonc`: **F** (Featurebase), **P** (Spin)
- Live-toggle slots (4): X, Q, A, Z
- General actions (4): `[`, `]`, `-`, `=`
- BTT (5, not in HS): digits 1–5 for Bear highlights

**So the "free" keys today, strictly reading your exclusions, come out to 16 letters** (Q, E, Y, U, O, A, G, H, J, K, L, Z, X, C, V, B) + digits 0, 6–9 + punctuation ``` ` \ ; ' , . / ``` — ~28 candidate slots.

## The big finding: Bear persistence already works in live slots

`bear-hud.lua` lines 540–593 — `handleLiveToggle` checks `isBearSlot(slot)` and:
- On re-open from closed: calls `openNoteInBear` + `restoreForNote` (same as `handleNoteHotkey`)
- On hide: `saveCurrentPosition()` before `minimize()`
- On raise-existing: `saveCurrentPosition()` of the *previously* focused note

The summon path (`handleLiveSummon`, 596–662) does the same. All four of your current slots (X/Q/A/Z) **are** Bear notes and already get full caret+scroll persistence. Nothing new to build — that concern is already solved.

## What Bear-launchers still have that live-slots don't

Two things, both tied to weekly notes:
1. **Template expansion** — `${weekNum}`, `${weekDays}` in `bear-notes.jsonc` get substituted at load; L005 rolls them weekly.
2. **R⌘ / R⌥ modifiers** — open past-week / next-week variants of D, W, T.

So Bear-launchers (N, R, D, W, T, S, M, I) still earn their dedicated slot. Expanding live-slots to the *rest* of the keyboard is purely additive — no refactor of the launcher system required.

## Pushback / things worth reconsidering

1. **"Just expand to every free key" has a silent-clobber problem.** With 4 slots and 4 keys of intent, a misfired R⌥+hyper+G is obviously wrong. With 16+ slots, it's easy to overwrite a slot you set last week and forgot about. `setLiveWindow` today just replaces and flashes yellow — no "are you overwriting X?" signal. Worth adding a guard (e.g. R⌥+hyper+key on an *already-set* key flashes red + prints the old value, requires a second press within 2s to confirm).

2. **The memory problem doesn't go away with more slots.** Your reasoning was "X/Q/A/Z is all I can remember." Moving to 16+ doesn't fix that — it just gives you capacity. The real lever is that L009-keymap now shows what's bound where. Worth being honest about whether you actually expect to *use* 16 slots, or whether the value is "no friction to add one when I need it."

3. **Bear-launchers vs. live-slots distinction is useful even post-expansion.** Keeping N/R/D/W/T/S/M/I in `bear-notes.jsonc` isn't just about memory — it'

[...truncated...]